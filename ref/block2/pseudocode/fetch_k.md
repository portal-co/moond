/* Generated by AI - C-like pseudocode. Normalize style. */

FETCH K — C-like pseudocode (Fetch instruction at K)

/* Fetch an instruction word from address K into staging buffers (B/SQ/Z as appropriate). */
void FETCH_K(address_t K) {
    word_t w = MEM.read(K); test_parity(w);
    B = w;
    SQ = opcode_of(w);
    Z = K + 1;

    // No STD2 here if fetch is part of another sequencing path; caller decides
}

/* TODO:VERIFY integration points and EXT handling. */