/* Generated by AI - C-like pseudocode. Normalize style. */

TCSA — C-like pseudocode (Test-set start at specified address)

/* Start execution at a specified address SA supplied by external test equipment (RSA/GSE). */
void TCSA(address_t SA) {
    // Read instruction word at SA (STMIC)
    word_t w = MEM.read(SA); test_parity(w);

    // Load sequencing registers and set next address
    B = w;
    SQ = ordercode_of(w);
    Z = SA + 1;

    // Transfer control to the fetched order code
    SQG.execute_STD2();
}
