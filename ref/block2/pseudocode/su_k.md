/* Generated by AI - C-like pseudocode. Normalize style. */

SU K — C-like pseudocode (Subtract memory K from A)

/* Subtract c(K) from A by adding the complement of K; update OVCTR via PINC/MINC signaling. */
void SU_K(address_t K) {
    word_t a = A;
    word_t k = MEM.read(K); test_parity(k); // STMIC read

    // Complement K per AGC subtraction semantics
    word_t k_comp = complement_word(k);

    sum_t result = add_with_flags(a, k_comp); // returns {value, overflow, underflow}
    A = result.value;

    if (result.overflow) signal_OVCTR_increment();
    else if (result.underflow) signal_OVCTR_decrement();

    SQG.execute_STD2();
}

/* Notes/TODOs:
 - TODO:VERIFY nuances of complement_word (15-bit vs 16-bit) and restore semantics for K when in E-memory.
 */
