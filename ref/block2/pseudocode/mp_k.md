/* Generated by AI - C-like pseudocode. Normalize style. */

MP K — C-like pseudocode (multiply A by memory K)

/* Multiply A by memory K using signed 14x14 multiply, store high into A and low into LP. AGCIS Issue 2 pp.46–60; AGCIS Issue 3 pp.3–11. */
void MP_K(address_t K) {
    // STMIC / memory inquiry
    word_t a = A;               // A operand (clear OV bit per AGCIS semantic before multiply)
    a = clear_OV_bit(a);       // inline helper to clear OV before multiply
    word_t k = MEM.read(K); test_parity(k); // operand parity check

    // Perform signed 14x14 multiply helper that returns high/low parts
    product_t p = signed_multiply_14x14(a, k); // canonical helper

    // Store results: high part into A, low part into LP per AGC conventions
    A = extract_high(p);
    LP = extract_low(p);

    // MP subinstruction sequencing (MP0..MP3) handled by SQG in hardware; schedule STD2 if required
    SQG.execute_STD2();
}

/* Notes/TODOs:
 - Uses signed_multiply_14x14 and extract_high/low helpers from guide.
 - TODO:VERIFY precise handling of sign/overflow bits and MPCTR interactions in Block-2 timing.
 */
