/* Generated by AI - C-like pseudocode. Normalize style. */

PINC — C-like pseudocode (increment addressed counter)

/* Increment counter at ctr_addr and notify Counter Priority Control on overflow. */
void PINC(address_t ctr_addr) {
    word_t v = MEM.read(ctr_addr); test_parity(v);

    // Counters are 15-bit quantities; increment with wrap
    word_t inc = (v + 1) & 0x7FFF;
    MEM.write(ctr_addr, set_parity_field(inc));

    // If wrapped around to zero (in 15-bit view) signal counter overflow
    if (inc == 0) notify_counter_overflow(ctr_addr); // TODO:VERIFY exact notification helper name/behavior
}

/* Notes/TODOs:
 - Uses set_parity_field before MEM.write per guide.
 - TODO:VERIFY counter-width and overflow detection semantics against AGCIS.
 */
