/* Generated by AI - C-like pseudocode. Normalize style. */

NDX E — Index Next Basic Instruction (Block-2)

/* Add contents of E into the instruction at I+1 to derive a new next instruction. */
void NDX_E(address_t E) {
    // STMIC / read indexing word
    word_t e = MEM.read(E); test_parity(e); // AGCIS NDX/EXTEND pages

    int16_t idx = sign_extend15(e);
    Instruction next_inst = fetch_instruction(I + 1); // returns order_code/address/raw_word

    Instruction derived = derive_instruction(next_inst, idx); // preserves EXT/quarter-code semantics

    // Load as the next instruction
    B = derived.raw_word;
    S = derived.address;
    SQ = derived.order_code;

    // Finalize sequencing via SQG
    SQG.execute_STD2();
}

/* Notes:
 - derive_instruction must perform bit-level addition and EXT handling per AGCIS.
 - TODO:VERIFY precise EXT timing per Block-2 conventions.
 */
