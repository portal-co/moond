/* Generated by AI - C-like pseudocode. Normalize style. */

MINC — C-like pseudocode (decrement addressed counter)

/* Decrement the counter at address CTR_ADDR by one; preserve 15-bit counter semantics and signal underflow via counter priority control if needed. AGCIS Issue 2 pp.86–102; AGCIS Issue 3 pp.3–11. */
void MINC(address_t ctr_addr) {
    // STMIC / memory inquiry
    word_t raw = MEM.read(ctr_addr); test_parity(raw); // AGCIS Issue 2 p.86–102

    // Treat counter as 15-bit signed/complement number per AGC conventions
    int32_t val = sign_extend15(raw);

    // Decrement
    val = val - 1;

    // Write back lower 15 bits with parity
    word_t out = (word_t)(val & 0x7FFF);
    MEM.write(ctr_addr, set_parity_field(out));

    // If counter wrapped to maximum (underflow condition) notify Counter Priority Control
    if (out == 0o77777) signal_counter_underflow(); // TODO:VERIFY exact detection condition per AGCIS

    // STD2 sequencing handled by SQG where instruction sequencing requires it
}

/* Notes/TODOs:
 - Uses sign_extend15 helper to maintain 15-bit semantics.
 - TODO:VERIFY — exact underflow/notification timing and whether MINC runs under Counter Priority Control path or inline.
*/
