
/* Generated by AI - C-like pseudocode. Normalize style. */

/* Generated by AI - C-like pseudocode. Normalize style. */

MINC — C-like pseudocode (decrement addressed counter)

void MINC(address_t ctr_addr) {
    // Read counter and check parity
    word_t e = MEM.read(ctr_addr); test_parity(e);

    // Arithmetic decrement on 15-bit counter
    uint32_t v = (uint32_t)(e & 0x7FFF);
    v = (v + 0x7FFF) & 0x7FFF; // subtract 1 with wrap for 15-bit

    word_t out = (word_t)(v & 0x7FFF);
    MEM.write(ctr_addr, set_parity_field(out));

    // If wrapped to maximum (underflow), signal Counter Priority Control
    if (out == 0x7FFF) signal_counter_underflow(); // TODO:VERIFY detection condition per AGCIS
}

/* Notes/TODOs:
 - Style: use C-like function signatures, helper functions for add_with_flags, multiply, divide, signal handlers.
 - Mark hardware-dependent or ambiguous behaviors with TODO:VERIFY.
*/
