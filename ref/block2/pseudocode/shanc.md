/* Generated by AI - C-like pseudocode. Normalize style. */

SHANC — C-like pseudocode (Shift and Add One)

/* Shift left the 15-bit content of the addressed counter and insert ONE into LSB; write back and signal priority control on overflow. */
void SHANC(address_t ctr_addr) {
    // STMIC / memory inquiry
    word_t v = MEM.read(ctr_addr); test_parity(v);

    // Shift left and insert one into LSB (15-bit)
    word_t out = ((v << 1) | 0x0001) & 0x7FFF;
    MEM.write(ctr_addr, set_parity_field(out));

    // If high bit of previous value was set, request program interrupt (UPRUPT)
    if (test_bit(v, 14)) signal_UPRUPT(); // TODO:VERIFY bit index mapping (14 vs 15) per AGC conventions

    // Overflow detection/handling
    if (detect_overflow_shift(v, out)) handle_shift_overflow(ctr_addr); // TODO:VERIFY handling details
}
