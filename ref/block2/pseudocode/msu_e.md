/* Generated by AI - C-like pseudocode. Normalize style. */

MSU E — Modular Subtract E (Block-2)

/* Compute modular TWO's-complement difference A - c(E) with cyclic semantics for angles. */
void MSU_E(address_t E) {
    // STMIC / memory inquiry
    word_t minuend = A;
    word_t subtrahend = MEM.read(E); test_parity(subtrahend);

    // Use helper that implements AGC cyclic two's-complement subtraction and normalization
    word_t result = twos_modular_subtract(minuend, subtrahend);
    A = result;

    // Finalize
    SQG.execute_STD2();
}

/* Notes/TODOs:
 - twos_modular_subtract should implement end-around-carry and sign normalization per AGCIS.
 - TODO:VERIFY treatment of overflow bit vs sign-bit for cyclic results.
 */
