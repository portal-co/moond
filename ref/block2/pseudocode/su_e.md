/* Generated by AI - C-like pseudocode. Normalize style. */

SU E — C-like pseudocode (Subtract E from A)

void SU_E(address_t E) {
    // Standard memory inquiry
    word_t a = A;
    word_t e = MEM.read(E); test_parity(e);

    sum_t r = add_with_flags(a, complement_word(e));
    A = r.value;
    if (r.overflow) signal_OVCTR_increment();
    else if (r.underflow) signal_OVCTR_decrement();

    SQG.execute_STD2();
}

/* TODO:VERIFY complement width and restore semantics for E-memory. */