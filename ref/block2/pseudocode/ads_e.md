/* Generated by AI - C-like pseudocode. Normalize style. */

ADS E — C-like pseudocode (Add and Set / ADS placeholder)

void ADS_E(address_t E) {
    word_t a = A;
    word_t e = MEM.read(E); test_parity(e);

    sum_t r = add_with_flags(a, e);
    A = r.value;
    if (r.overflow) signal_OVCTR_increment();
    else if (r.underflow) signal_OVCTR_decrement();

    SQG.execute_STD2();
}

/* TODO:VERIFY ADS vs AD differences in AGCIS. */